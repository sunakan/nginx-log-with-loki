[SERVICE]
    flush        1
    daemon       Off
    log_level    info
    parsers_file parsers.conf
[INPUT]
    Name   tail
    Path   /var/log/nginx/access.log
    Parser nginx-ltsv
    Tag    nginx.access
[OUTPUT]
    Name   loki
    Match  *
    Host   loki
    Port   3100
    Labels job=$tag
