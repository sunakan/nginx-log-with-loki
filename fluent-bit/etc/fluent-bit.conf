[SERVICE]
    flush        1
    daemon       Off
    log_level    info
    parsers_file parsers.conf
[INPUT]
    Name tail
    Path /var/log/nginx/access.log
    Tag  nginx.access
[OUTPUT]
    name  stdout
    match *
